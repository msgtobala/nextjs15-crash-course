'use client';
import React, { JSX } from 'react';

const GlobalError: React.FC = (): JSX.Element => {
  return (
    <html>
      <body>
        <p>Error</p>
      </body>
    </html>
  );
};

export default GlobalError;
